---
import { withBase } from "../lib/url";

// The glossary now lives as a section on /standards (site consolidated
// to 5 primary pages). Redirect anyone with the old URL bookmarked.
return Astro.redirect(withBase("/standards#glossary"), 308);
---
